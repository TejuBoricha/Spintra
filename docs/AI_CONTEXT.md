# AI_CONTEXT.md — Spintra Project Living Memory
> The authoritative source of truth for the **current state** of the project — nothing else.
> Historical implementation details live in `CHANGELOG_AI.md`. Architecture, tech stack, and the
> DB schema live in `ARCHITECTURE.md`. Session-to-session handoff lives in `HANDOFF.md`. Backlog
> and roadmap live in `TASKS.md`. Do not duplicate those here — link to them instead.
> Always update this file after every significant milestone.
> Last updated: 2026-07-17 IST

---

## Current Milestone

Pre-launch hardening tier: Legal Basics (30, placeholders filled 36), Rate Limiting (31), Abuse & Moderation Controls (32) are done and live. Production Error Monitoring is explicitly deferred by the user's own choice (told them it's a visibility gap, not a launch blocker; they chose to skip it for now) — not an oversight, don't pick it up unprompted. Sessions 30–33 were committed as 4 scoped commits and pushed. That push's CI run then surfaced a second, wholly pre-existing bug (unrelated to Sessions 30–33): rooms created without Supabase configured never auto-activated their game — fixed in Session 34. Session 35 triaged the repo's 5 open Dependabot PRs: merged 4 safe GitHub Actions bumps, and applied 15 of 16 bundled npm updates directly to `main` while holding back `eslint ^10` (upstream-incompatible with `eslint-config-next`). Session 36 filled in the legal pages' real operator/jurisdiction/contact info. **Session 37: full Product Readiness Audit** (33 findings across functional/integration/DB-security/state-management/UX passes) followed by fixing the 1 Critical + 6 High findings — most notably, the multiplayer Tournament room activity could never actually finish a tournament (fixed by extracting a shared `src/lib/tournament-engine.ts` used by both the standalone tool and the new room activity). **Session 38: worked through the remaining backlog** — all 6 Medium findings (RLS column restrictions, DB-level room lock enforcement, missing constraints/index, message-reports host UI, presence reconciliation, react-query removal) plus most of the 15 Low findings. Also discovered and fixed a real production gap along the way: migrations 0008/0010 were tracked as applied but had never actually run (a genuine SQL syntax bug in 0010) — `activity_prompts`/`trivia_questions` didn't exist live until this session, though 5 activities' graceful hardcoded fallbacks meant no real user-facing outage. **Session 39: 13-area platform QA audit** — fixed Live Trending Rooms (auth init), privacy bypass via Recent Activity (is_public filter), broken explore filters (real participant counts, queueMicrotask cutoff), banned-user toast inconsistency (homepage + explore), fake homepage stats (10,000+ rooms, unimplemented chat features), tournament integrity (tie rejection, TBD non-clickable, completed-match re-edit guard), Party vs Classroom distinction (classroomSafe game tagging + picker filter), and pre-existing CRLF bug in docs drift check. A handful of Low findings, room auto-expiry (new Medium Priority), and the 3 larger net-new Medium features (Visual Scoreboard, XP/Leveling, Room Settings Panel) remain queued in `TASKS.md`. **Session 40: Room auto-expiry implemented + full migration audit** — migration `0009` (its `is_member_of_room` helper, hardened RLS select policies, participant-limit trigger, and the `cleanup_inactive_rooms()` function) was, like `0008`/`0010` before it, tracked as "applied" but had never actually executed live; re-running it for real deleted 23 genuinely abandoned rooms on the spot. New migration `0020` enables `pg_cron` and schedules `cleanup_inactive_rooms()` every 30 minutes, closing the room-persists-indefinitely gap from Session 39 without needing Supabase dashboard access. Followed up by systematically cross-checking all 20 migrations' expected live objects against the actual database (functions, triggers, policies, constraints, indexes, extensions, realtime publication membership, replica identity, seed-data row counts) — confirmed no further tracked-but-never-ran gaps exist beyond the three already found and fixed (`0008`, `0009`, `0010`).

**Session 45: Full Product/UX/Engineering/Security/Production-Readiness Audit (15-phase, 5 parallel domain agents) + fixing all findings tier-by-tier — COMPLETE.** Re-verified all 64 previously-fixed findings from Sessions 37-43 (all still correctly in place). Found 42 new findings (1 Critical, 4 High, 9 Medium, 28 Low). **Critical tier — fully fixed and verified:** Supabase Realtime Broadcast/Presence channels (`room:{code}`) had zero authorization, letting any anonymous session observe/forge events in any room including private ones, and letting a banned/kicked user keep full realtime access. Fixed via migration `0036` (Realtime Authorization — RLS on `realtime.messages`, scoped by the existing `is_member_of_room()` helper) plus a client change creating the channel with `{ config: { private: true } }`, gated on a new `participantRowReady` flag so a client never subscribes before its own `room_participants` row exists. Live e2e verification (Docker/WSL installed mid-session specifically for this, giving access to the project's own ephemeral local Supabase stack after live testing against production proved confounded by rate-limit pressure) caught two secondary bugs this introduced before they could ship: (1) delaying the host's own subscribe widened a window where a fast guest-join event could be missed — closed with a one-time reconciliation fetch on `SUBSCRIBED`; (2) more seriously, it widened a race in the pre-existing presence-reconciliation logic (migration 0019), causing a real, reproducible duplicate-host bug (confirmed via direct DB inspection: two rows with `role='host'` in the same room) — fixed by skipping the crash-reconciliation write on each channel's first sync only. **High (4/4), Medium (8/9), Low (26/28)** — full detail in `TASKS.md`'s Session 45 section and `CHANGELOG_AI.md`. Migrations `0036`-`0039` all applied and verified live. `npm run verify`/`npm run build` clean throughout; 7/7 Playwright tests pass repeatably against a freshly-reset local instance.

**Session 46: Post-Session-45 CI fixes + all 5 remaining Session 45 findings — COMPLETE.** Checking CI status on Session 45's push found `validate` failing; investigated rather than assumed a Session 45 regression, and found two unrelated pre-existing bugs, both fixed and pushed as their own commits first: (1) `validate` had failed since `752295f` (2026-07-06) — a build-time env var assertion in `next.config.ts` conflicted with that job's intentional no-Supabase demo-mode build, fixed with an explicit `SKIP_ENV_VALIDATION` opt-out; (2) that fix let the job's tests actually run for the first time in days, surfacing a second bug — demo-mode rooms never auto-activated their chosen game (dead code since `c0f1798`, 2026-07-04), fixed by removing a redundant outer effect guard that made an existing demo-mode branch unreachable. Then, at the user's request, all 5 items Session 45 had left open were resolved: **analytics/telemetry** (migration `0041`, `analytics_events` table — insert-only RLS, no select policy, rate-limited — plus `src/lib/analytics.ts`; deliberately scoped to 3 events: `room_created`/`room_joined`/`activity_started`, not full instrumentation); **homepage 3D hero unconditional load** (now gated on `useReducedMotion()` + an `IntersectionObserver`, so the WebGL render loop stops once scrolled out of view); **departed/kicked users' chat showing "Guest"** (migration `0040` adds a `username` snapshot column to `chat_messages`, captured at send time); **Truth or Dare content fork** (unified via a shared `TRUTH_OR_DARE_CATEGORIES` source in `src/lib/utils.ts`, without reworking the tool page's zero-network-dependency architecture as originally feared); **`tm_teams`/`nd_winner` event-kind naming** (renamed to `team_maker_teams`/`name_draw_winner`, with both old kind strings kept as read-only legacy `ActivityEvent` union members so already-persisted `room_activity_state` rows still replay). All migrations applied and verified against a local Docker Supabase stack (`supabase db reset` + full Playwright run, 7/7 passing); directly confirmed via `psql` that `analytics_events` and `chat_messages.username` are populated by real app usage. `npm run verify`/`npm run build` clean with production credentials restored. Local Docker/Supabase stack stopped after verification; Docker itself kept installed (permanent part of this repo's verification toolkit, matching `ARCHITECTURE.md` §9's `db-integration` CI job). A follow-up fix landed the same day: joining a 2-person-capped room as a genuine 2nd participant raised a false "room full" error — `check_room_limit_before_join()` counted the joining user's own row against their own capacity check on a redundant upsert (reliably reproduced via React Strict Mode's dev-only double-invocation); fixed via migration `0042`, excluding the caller's own row from the count.

**Session 47: Unban UI, empty-state copy pass, expanded e2e coverage — COMPLETE.** Closed the 3 remaining smaller items from the audit's UX×10/Product×10 lists — host-facing unban list (Session 45 deferred), consistent empty-state copy across all 14 activities (never addressed), reconnect/presence-reconciliation e2e coverage (Session 45 left open) — explicitly scoped away from the 4 larger net-new features and 2 user-action-only items. Planned via 3 parallel Explore agents + a Plan agent before implementation. Delivered: migration `0043` (host-scoped select/delete policies + `username` snapshot on `room_bans`) and a new `unban-panel.tsx` cloning `MessageReportsPanel`'s pattern; standardized "waiting for host" copy across all 14 activities plus host-idle captions for the 7 that had none, with genuine "not enough players" guards added only to Team Maker and RPS (the only 2 where 0/1 online participants produced a broken/misleading result — the other 9 work correctly solo); 2 new `multiplayer-loop.spec.ts` tests (reconnect via same-context `page.reload()`, presence-reconciliation via a 3rd participant joining then leaving cleanly). **3 real, previously-unknown bugs found and fixed via actually running the new code**: (1) first-time joiners of an in-progress room never saw the in-progress activity state — `room_activity_state`的 RLS requires an existing participant row, but `loadRoomDetails()` read it before `trackSelf()` created that row; fixed by reordering `runSetup`. (2) `room_bans` was never added to the `supabase_realtime` publication (unlike `message_reports`), so the new unban panel's list could never live-update; fixed in migration `0043`. (3) realtime-joined participants got a malformed `participants` entry (missing the nested `user` object, breaking their displayed name/avatar) because the `postgres_changes` INSERT handler pushed the raw DB row straight into state instead of shaping it like `loadParticipants()` does; fixed to match. All migrations applied fresh via local Docker Supabase, full Playwright suite (9/9) run twice; unban feature manually smoke-tested end-to-end (this manual pass is what caught bugs #2 and #3). `npm run verify` clean; migration `0043` pushed live and re-verified.

**Session 48: Audit Issues Implementation — COMPLETE.** Completed the tier-by-tier fix mandate for all issues identified in the comprehensive production readiness audit:
- **Phase 1 (DB, Backend, Security)**: Resolved the Explore page table-wide subscriptions DDOS vector by adding a denormalized `rooms.participant_count` column (migration `0044`) and triggers on `room_participants`. Secured Trivia question answers by revoking SELECT privileges on `trivia_questions(correct_index)` (migration `0045`) and verifying guesses via a secure database RPC.
- **Phase 2 (Reliability, UX & Error Recovery)**: Refactored host election to execute via an atomic database RPC (`elect_room_host`, migration `0046`), preventing split-brain states; added layout-level solo play warning banners (`src/app/tools/layout.tsx`) to all `/tools/*` pages; optimized chat performance by extracting and memoizing `ChatMessageItem` to stop typing keystroke re-render cascades; synchronized Bingo card layouts in `localStorage` keyed by room code to survive page reloads and reconnects.
- **Phase 3 (Accessibility, Polish & UX)**: Implemented "Recently Visited Rooms" list on the home screen synced to `localStorage`; converted room join headers to semantic `<label>` tags and added high-visibility keyboard focus rings; integrated "Leave Room" buttons/dialog modals in the room header and registered `beforeunload` listeners to prevent accidental session loss.
All migrations applied locally, database types regenerated, and successfully pushed to the remote production instance. The entire Playwright E2E suite is green and verified.

**Session 49: Room Settings Panel — first net-new backlog feature, analysis-first — COMPLETE.** First net-new feature from the Session 48 backlog, built through a deliberate Business-Analysis-first process (codebase-grounded BA on all four backlog items, then decision-by-decision analysis for Room Settings, recorded as ADR-007). Delivered: migration `0049` (`2 ≤ max_participants ≤ 50` CHECK constraint, server-authoritative — the creation slider, the panel, and any raw API call now share one DB-enforced bound); new `room-settings-panel.tsx` (name/capacity/visibility/lock); wired into `room-header.tsx`. Migration 0049 applied live and verified directly on the production database via `pg_constraint`/`pg_get_constraintdef`. Committed as `0c984b8` on `feat/room-settings-panel`. e2e suite 11/11 passing. Full detail: `docs/TASKS.md` §Session 49.

**Session 53: Comprehensive Release Candidate (RC) Audit — COMPLETE.** Conducted a thorough end-to-end audit of all 18 development, QA, and security phases. Results compiled in `final_release_audit.md`. Tested current product build against local and remote Supabase endpoints. Playwright integration tests run, validating core multiplayer loops, scoreboard standups, XP preservation, and moderation workflows.
**Session 54: Tournament QA Automation Audit — COMPLETE.** Performed a comprehensive QA and engineering audit of the Spintra Tournament system. Discovered 12 defects and architectural risks (saved to `tournament_qa_audit_report.md` in the brain artifacts directory). Created and executed a comprehensive automated E2E and unit test suite (`tests/comprehensive-tournament-audit.spec.ts`) validating 48 scenarios (38 unit matrix/out-of-bounds/scoring tests, and 10 E2E/Multiplayer UI checks, including duplicate names, negative score values, and whitespace validations). All 48 tests passed successfully.

**Session 61: Concurrent multiplayer stress-testing + production readiness + first real deployment — COMPLETE.** Prompted directly by user pushback after several real bugs slipped past prior "verified live" claims — the user asked why bugs kept surfacing in code that had supposedly been tested, and the honest answer was that "verified live" had meant narrow single-scenario checks, never genuine concurrent multi-client load. This session ran real 2-3-client Playwright sessions against the live Supabase project (not mocked) specifically hunting for concurrency bugs, and found three:

1. **Host-election split-brain**: `elect_room_host` (0046/0056) had no locking and wasn't idempotent — two clients racing to take over from a crashed host could both end up `role='host'` (observed live: two participant rows both `role='host'`, `rooms.host_id` landing on whichever committed last). Also caused triple-duplicate "You are now the host" toasts from a single client's own racing election calls. Fixed with `pg_advisory_xact_lock(hashtext(room_code))` serializing election attempts per room, migration `0061`.
2. **Healthy peers falsely marked offline**: presence-based crash-reconciliation trusted a single `channel.presenceState()` snapshot as ground truth; a transient under-report right after any peer's connection state changed could get a genuinely-connected survivor (once, the one just promoted host) written `is_online:false`. Fixed with a confirm-after-4s-with-fresh-recheck pattern instead of acting on one snapshot.
3. **Realtime channel torn down on every game answer**: `useRoomSubscription`'s channel-setup effect (owns the entire realtime channel) depended on the whole `currentUser` object; `awardScore()` replaces `localUser` with a new object on every trivia/rps/bingo answer solely to update xp/rank, tearing the channel down and rebuilding it on every single answer — the "Realtime subscription failed" toast a real two-player session hit repeatedly. Narrowed the effect's dependency to `currentUser.id`/`.username`, the only fields it actually reads.

All three verified via repeated live multi-client runs (6 clean host-crash simulations, 6 rounds of concurrent trivia answering with an exactly-correct XP ledger). RPS/Bingo/Tournament/Moderation/activity-switching were also stress-tested and found correct (one RPS "failure" was a test-design bug — host never submitted a choice, not an app bug). Two narrow findings recorded but not fixed (user's call): Bingo's async dual-winner race (code-reviewed, not live-reproduced) and a cosmetic duplicate `moderation_actions` audit-log entry on concurrent double-kick — both in `TASKS.md`.

Same session, pivoted to production readiness: `deploy.yml` and `db-backup.yml` had zero repo secrets configured and had been silently failing on every run (deploy: since creation; backups: 5+ consecutive days, confirmed via `gh run list`) — both now configured (Supabase access token/DB password/project ID; Cloudflare R2 for backup storage, chosen over AWS S3 for its free tier and zero egress fees) and verified with real successful runs, including a genuine 410 KiB backup upload. The `db-backup.yml` fix took 3 follow-up rounds after the initial secret configuration — a Postgres server/client version mismatch (server runs 17.6, workflow was pinned to `postgresql-client-16`), a missing PGDG apt repository for the newer client version, and apt-installing v17 without it becoming the binary actually invoked (bare `pg_dump` still resolved to Ubuntu's pre-installed v16) — each only surfaced by actually running the workflow, not by reading it. Also found and fixed while wiring up Sentry: `sentry.client.config.ts` (the SDK's original scaffolding, from an earlier session) has never actually worked under Turbopack, which this project's dev/build always runs under — confirmed via the SDK's own webpack.js deprecation warning, and empirically (the DSN never appeared in any served client JS chunk). Fixed by moving init to `src/instrumentation-client.ts`; verified live against a real Sentry project.

Session closed with the actual first production deployment: Vercel project created, connected to this GitHub repo, environment variables configured, Vercel Authentication (which was blocking all public access by default) disabled, custom domain `spintra.io` connected via Cloudflare DNS (CNAME, DNS-only/unproxied per Vercel's requirement), and the default `.vercel.app` alias set to redirect (308) to `spintra.io` as the one canonical URL. Verified end-to-end against the live production URL: `/api/health` reachable, a real room created successfully against production Supabase, zero console/network errors.

**Session 62: Google Analytics (GA4) Integration — COMPLETE.** User asked to add Google Analytics. Before writing any code, found a direct conflict: the Privacy Policy and the cookie-consent banner both explicitly promised "no advertising or third-party tracking," which standard GA4 (third-party `_ga`/`_gid` cookies, data sent to Google) would make false. Asked the user whether GA should fire unconditionally (matching the existing Sentry pattern — no gate, just accurate disclosure) or be held behind a real accept/decline choice; user chose unconditional firing with the legal copy corrected instead. Delivered: `NEXT_PUBLIC_GA_MEASUREMENT_ID` env var, optional and no-op when absent (same degrade-gracefully pattern as Sentry's DSN); `gtag.js` loaded via `next/script` in `layout.tsx`, gated on that var; `next.config.ts`'s CSP `script-src` allowlists `googletagmanager.com` only when the var is set, so an unconfigured deployment's CSP stays byte-for-byte identical to before this integration (`connect-src` needed no change — it already allows any `https:` origin, which covers gtag.js's own calls to Google's collection endpoints); Privacy Policy §1/§3 and the cookie banner rewritten to disclose GA and its cookies instead of denying third-party tracking; the now-stale comment in `src/lib/analytics.ts` (which cited the old "no third-party tool" promise to explain why that file exists) corrected to describe the actual, still-true reason that file is separate from GA (first-party product-event telemetry GA can't provide). Verified live via two real dev-server runs, not just typecheck: with no Measurement ID set, the response's CSP header and served HTML are unchanged from the pre-change baseline (diffed via `curl`); with a test ID set, the CSP grows exactly the one expected `googletagmanager.com` directive and the gtag script tag/config call both appear in the served HTML. `npm run verify` and `npm run build` clean (0 errors; 4 pre-existing warnings in unrelated files, untouched by this change). **Now activated in production:** the real Measurement ID `G-0XRPFF5MCD` was set in `.env.local` and in Vercel's Production environment (non-sensitive, since a GA ID ships publicly in page HTML), the branch merged to `main` (`b48e55d`) and auto-deployed by Vercel, and confirmed serving live — `curl https://spintra.io/` returns the `gtag.js` script + `gtag('config', 'G-0XRPFF5MCD')` call and the production CSP now carries the `googletagmanager.com` `script-src` entry; the updated Privacy Policy (July 15 effective date, GA disclosure) is live too. Only the GA-side Realtime confirmation (the user visiting the live site and seeing themselves in GA's Realtime report) remains — a user action, not a code/deploy step.

**Session 63: Tool-page SEO content sections — COMPLETE (working tree; not yet committed/deployed).** User asked how to bring more traffic to the site. Highest-leverage code lever identified and built: the 14 standalone `/tools/*` pages target massive evergreen search demand ("wheel spinner", "random name picker", "team generator", "dice roller", …) but each rendered only its interactive widget (~30 words of crawlable text) — too thin to rank against wheelofnames/pickerwheel. Added a central per-tool content registry (`src/lib/tool-seo-content.ts`, keyed by the same href as `toolMetadata()`) and a pure server component (`src/components/tool-seo-section.tsx`) that renders an intro + how-to + use-cases + FAQ below each widget and emits `FAQPage` JSON-LD, with related-tool internal links. Wired into all 14 tool `layout.tsx` files. Zero added client JS (native `<details>`, server component). Verified against real `next build` output — all 14 tool routes prerender as static `○`, and the FAQ copy + schema + internal links are confirmed present in the emitted static HTML. `npm run verify`/`npm run build` clean. Non-code growth levers (teacher-community distribution, per-tool OG images, Google Search Console setup, community seeding) were discussed and logged in `TASKS.md` but not built. **Follow-up same session:** also enriched all 14 tool pages' above-the-fold subtitle `<p>` to lead with the head search term (e.g. "A free online spinner wheel — …"), keeping the branded H1s intact — actual H1 renames are deferred to the user as a branding call. Both changes verified via `npm run build` (all 14 tool routes prerender static; head-term copy + FAQ schema confirmed in emitted HTML). **Committed (`f7a9da3`) and pushed to `main` — deployed live.** Confirmed via direct `curl` against `https://spintra.io`: sitemap.xml now lists `/tools` (20 URLs), the RPS page serves the corrected vs-computer copy, Lucky Wheel serves its new subtitle, and the response is a fresh (`Age: 0`) Vercel prerender, not a stale cache hit.

**Session 63 (continued): three more fixes, all merged to `main` (Vercel auto-deploys on every push to `main`).** `/api/health` hardened for external uptime monitoring — the database/auth/realtime checks now run concurrently via `Promise.all` (previously sequential, risking a monitor's own timeout during a partial outage), the database check gained the same 5s `AbortSignal.timeout` the other two already had, and the response sets `Cache-Control: no-store`. Per-tool OpenGraph images added for all 14 `/tools/*` pages (`src/lib/og-image.tsx` + 14 `opengraph-image.tsx` files; gradient hex values read from the real running app via Playwright/`culori` rather than a memorized Tailwind palette, since v4's defaults differ from v3's) — each tool now gets its own share-card image instead of one generic banner. **Then a Critical security finding surfaced and was fixed the same session:** `rooms_select` had allowed any anon-key holder to list every room including private ones (with the join-code exposed) since migration `0005` — fixed via migration `0062` (tightened the policy to the same `is_public`/host/member pattern used elsewhere, added a `get_room_by_code` security-definer RPC for the legitimate pre-join lookup; 6 client call sites converted). Verified against a local Docker reset first (63/63 Playwright), then **applied directly to production via `supabase db push` and independently confirmed live** — broad enumeration now returns empty, `get_room_by_code` still resolves a real room by its exact code. Committed as `f73ccef`/`198c300`/`de6b462`, all on `main`.

**Session 64: `/for-teachers` landing page — COMPLETE, committed and pushed to `main` (`72ad257`), deployed.** User asked to make the site "go viral"; also asked to check Google Analytics for real traffic data — declined rather than guessed, since there's no connected GA4 tool in this environment (only Gmail/Calendar/Drive are wired up). Built the last open item in `TASKS.md`'s Growth backlog instead: a new static `src/app/for-teachers/page.tsx` (hero, trust points, a curated 6-tool "classroom ideas" section, the full `GAMES.filter(g => g.classroomSafe)` grid, an FAQ with `FAQPage` JSON-LD, closing CTA), its own `opengraph-image.tsx` (reuses Bingo's already Playwright-verified gradient rather than guessing a fresh `sky-500` hex, the exact mistake Session 63 avoided), a non-breaking refactor extracting `renderOgImage()` out of `src/lib/og-image.tsx` so the 14 existing tool OG images are byte-for-byte unaffected, `/for-teachers` added to `sitemap.ts`, and a homepage footer link. **Checked before writing content:** `legal/privacy/page.tsx` §6 states the Service isn't directed at children under 13 — the page is deliberately framed around the teacher as operator, with zero new compliance claims, only facts the Privacy Policy/`ARCHITECTURE.md` already back. Verified via `npm run verify`/`npm run build` (both clean; `/for-teachers` + its OG image prerender static) and a real dev-server pass driven with Playwright — screenshotted light/dark, clicked a FAQ item open, fetched and visually inspected the OG image, zero console errors. User confirmed to commit and push; done (`72ad257`, on `main`).

---

## Overall Progress

All planned modularisation (14/14 activities), invite and QR sharing systems, realtime discovery feeds, client profile synchronizations, database-driven activity prompt migrations, security hardening, CI/dependency automation, visual scoreboards, XP levels engines, room settings, moderation action panels, and documentation-workflow work is complete. Build/lint/typecheck are clean. The comprehensive test suite validates the current system.

---

## Current Objective

**Spintra is live in production** at https://spintra.io (Vercel, custom domain via Cloudflare DNS) as of Session 61 — this is a genuine change from prior sessions' "ready for production launch," which described code readiness without an actual deployment target. Migrations auto-deploy on merge to `main` (`deploy.yml`), daily DB backups actually succeed and land in Cloudflare R2 (`db-backup.yml`), and Sentry error monitoring is live and verified. The multiplayer core has been stress-tested under real concurrent multi-client load, not just single-session manual checks.

---

## Current Focus

Monitor Sentry for real production errors now that strangers (not just known testers) can reach the site. Watch `deploy.yml`/`db-backup.yml` for continued success (both were silently broken for a long time before Session 61 — don't assume a past green run means the next one will be).

---

## Known Issues

- **`eslint` intentionally pinned at `^9`.** Dependabot will keep proposing `^10`; do not accept until `eslint-config-next`'s bundled `eslint-plugin-react` ships ESLint 10 support upstream. Confirmed (Session 35) that upgrading crashes `npm run lint` with `TypeError: contextOrFilename.getFilename is not a function` — a removed ESLint 10 API that plugin still calls internally. Verify by installing the bump in a scratch worktree and running `npm run lint` directly, not just by trusting a green CI badge on an unrelated branch.
- **Rate limiting and bans (migrations 0011/0012) are bypassable by rotating the anonymous session.** Both key on `auth.uid()`; since every user is an anonymous, unverified session, clearing browser storage (or an incognito window) resets both instantly. This is an accepted architectural trade-off (frictionless onboarding was a deliberate choice, see migration `0001`'s header comment), not a bug in the trigger logic — but treat it as "slows down casual abuse," not a real defense against a motivated abuser. Found and explicitly documented in the Session 37 audit; previously an implicit assumption, not written down anywhere.
- **Local CRLF reproduction of CI is unreliable — use `git show`, not clone/checkout.** This machine's global `core.autocrlf=true` smudges LF→CRLF on *any* operation that materializes files on disk (`git clone`, `git archive`, `git checkout-index`), not just the primary working tree — so a "fresh clone to /tmp" still doesn't show what a real Linux CI checkout sees. The only reliable way to inspect true blob content is `git show HEAD:<path>` (or `git cat-file`), which bypasses the smudge filter entirely. Learned the hard way in Session 37 when a genuine `docs:check` failure (a missing migration in `ARCHITECTURE.md`'s table) was initially misdiagnosed as the usual CRLF false positive.
- **Trivia's answer key is world-readable via RLS.** `trivia_questions.correct_index` has a `using (true)` select policy, so any client can fetch the answer before playing. Not fixed — would need a server-side answer-check RPC, disproportionate effort for a casual trivia game at this scale. Documented in `TASKS.md`, found in the Session 37 audit.
- **Chat profanity filter is client-side only.** `src/lib/chat-filter.ts` runs before insert client-side; a direct Supabase call bypasses it entirely. Not fixed, same reasoning as above (low severity, disproportionate effort to fix properly). Documented in `TASKS.md`.
- **Host-election "earliest joined" tiebreak isn't DB-enforced.** The client picks the earliest-joined online participant to self-promote, but the DB trigger only checks "no other online host exists" — a race could let any online participant self-promote first, not necessarily the earliest. Rare, cosmetic fairness issue only, not a security concern. Not fixed.
- **Multiple room membership is not prevented.** A single anonymous user can join multiple rooms simultaneously — there is no server-side enforcement preventing this. Accepted architectural trade-off of the anonymous identity model (frictionless onboarding over strict session constraints). Documented in Session 39; not a launch blocker.
- **Rooms persist indefinitely** — **fixed Session 40.** `pg_cron` now runs `public.cleanup_inactive_rooms()` (rooms with no online participants, >2h old) every 30 minutes via migration `0020`.
- **A real, already-merged "Session 44" exists in `git log`** (13+ commits) whose work was never reflected in `AI_CONTEXT.md` at all. Reconciled as numbering drift due to multiple AI agent executions.
- **Bingo's async win-verification has no arbitration between two simultaneous valid winners.** Found via code review during Session 61's stress-testing pass, not live-reproduced — deliberately not chased further (see `TASKS.md` for the exact mechanism and why).
- **Concurrent double-kick can write a duplicate `moderation_actions` audit-log entry.** Found live during Session 61 — cosmetic only, the actual ban state is never duplicated. See `TASKS.md`.
- **"Verified live" in past session notes throughout this file described single-scenario checks, not genuine concurrent multi-client load** (the gap Session 61 exists to close for the multiplayer core specifically). Treat older "verified live"/"confirmed working" claims about realtime/host-election/presence behavior with that caveat unless a session explicitly says it used multiple simultaneous real clients.
- **Rooms table privacy bypass — fixed and deployed to production (Session 63).** `rooms_select` had been `using (true)` since migration `0005`, letting any anon-key holder list every room via the raw REST API (private ones included) with each room's `code` — the actual join credential — fully exposed. Migration `0062` tightened the policy to the same `is_public`/host/member pattern already used on `room_participants`/`chat_messages`, and added a `get_room_by_code` security-definer RPC for the legitimate pre-join lookup case (6 client call sites converted). Verified against a local Docker reset first (63/63 Playwright), then **applied directly to production via `supabase db push` and independently confirmed live** (`de6b462`) — broad enumeration now returns empty, `get_room_by_code` still resolves a real room by its exact code. See `TASKS.md` and `CHANGELOG_AI.md` Session 63 for full detail.
- **Google Analytics is live in production** (Session 62), measurement ID `G-0XRPFF5MCD` (Vercel Production env + `.env.local`). **Consent is gated (Google Consent Mode v2)** — GA loads but `analytics_storage` defaults to `'denied'`, so no `_ga` cookie is set and no data is sent until the visitor clicks **Accept** in the cookie banner (Decline keeps it off; the choice persists via `localStorage['spintra-cookie-consent']`). Wired in `src/app/layout.tsx` (consent default before `config` + stored-grant replay) and `src/components/cookie-consent-banner.tsx` (Accept/Decline). **Merged to `main` (`2098913`) and deployed** — Vercel auto-deploys on every push to `main`, so the live site now defaults to denied consent, not the original unconditional-firing behavior. When GA is unconfigured the banner is a plain informational notice (no analytics). See `CHANGELOG_AI.md` Session 62 follow-ups.

---

## Current Assumptions

Load-bearing assumptions a new session should be aware of before making changes:

- **No verified user identity.** Every client is a Supabase anonymous auth session (`auth.uid()`) or, if Supabase isn't configured, a random `localStorage` ID — never a verified account. RLS restricts *what shape of data* a request can touch, but cannot confirm a client is who it claims to be. Treat this as "good enough to stop casual abuse," not a secure identity system (see migration `0001`'s header comment).
- **BroadcastChannel fallback is same-browser-only.** When Supabase env vars are absent, multiplayer "works" only across tabs in the same browser — it does not sync across devices or real users. Local-dev/demo convenience only.
- **No session persistence across visits.** Closing a room tab and reopening it does not restore a prior session beyond whatever `localStorage` already holds.
- **Static prompt/question content is English-only, no i18n.**

---

## Next Recommended Task

Visual Scoreboard, XP/Leveling, and a real Moderation Dashboard (beyond direct SQL-editor querying) — all listed as net-new in earlier sessions — **have since shipped** (see PR #24, "new design system, settings page, and moderation dashboard v2"); this file's prior "Next Recommended Task" section was stale on that point and has been corrected.

Session 64's `/for-teachers` page is live on spintra.io. Non-code distribution (seeding r/Teachers, teacher Facebook groups, Pinterest) is still on the user, not something a future session can do. Also open: the user asked for a GA4 traffic/conversion review that couldn't be done here for lack of a connected tool — worth revisiting if the user shares numbers or grants access.

The two narrow items from Session 61 (Bingo dual-winner race, duplicate audit-log entry — both in `TASKS.md`) are explicitly deferred by the user's own choice, not oversights — don't pick them up unprompted. Reasonable next threads if asked "what's next": watch Sentry for real production error patterns now that strangers can reach the site; consider whether the Bingo/audit-log items are worth fixing once there's real usage data on how often they'd actually trigger.

---

## See Also

- **Architecture, tech stack, DB schema/ER diagram, coding standards** → `ARCHITECTURE.md`
- **Why a past decision was made** → `DECISIONS.md`
- **Full chronological implementation history** → `CHANGELOG_AI.md`
- **Exactly where the previous session stopped** → `HANDOFF.md`
- **Backlog, technical debt, roadmap, priority** → `TASKS.md`

# AI_CONTEXT.md — Spintra Project Living Memory
> The authoritative source of truth for the **current state** of the project — nothing else.
> Historical implementation details live in `CHANGELOG_AI.md`. Architecture, tech stack, and the
> DB schema live in `ARCHITECTURE.md`. Session-to-session handoff lives in `HANDOFF.md`. Backlog
> and roadmap live in `TASKS.md`. Do not duplicate those here — link to them instead.
> Always update this file after every significant milestone.
> Last updated: 2026-07-09 IST

---

## Current Milestone

Pre-launch hardening tier: Legal Basics (30, placeholders filled 36), Rate Limiting (31), Abuse & Moderation Controls (32) are done and live. Production Error Monitoring is explicitly deferred by the user's own choice (told them it's a visibility gap, not a launch blocker; they chose to skip it for now) — not an oversight, don't pick it up unprompted. Sessions 30–33 were committed as 4 scoped commits and pushed. That push's CI run then surfaced a second, wholly pre-existing bug (unrelated to Sessions 30–33): rooms created without Supabase configured never auto-activated their game — fixed in Session 34. Session 35 triaged the repo's 5 open Dependabot PRs: merged 4 safe GitHub Actions bumps, and applied 15 of 16 bundled npm updates directly to `main` while holding back `eslint ^10` (upstream-incompatible with `eslint-config-next`). Session 36 filled in the legal pages' real operator/jurisdiction/contact info. **Session 37: full Product Readiness Audit** (33 findings across functional/integration/DB-security/state-management/UX passes) followed by fixing the 1 Critical + 6 High findings — most notably, the multiplayer Tournament room activity could never actually finish a tournament (fixed by extracting a shared `src/lib/tournament-engine.ts` used by both the standalone tool and the new room activity). **Session 38: worked through the remaining backlog** — all 6 Medium findings (RLS column restrictions, DB-level room lock enforcement, missing constraints/index, message-reports host UI, presence reconciliation, react-query removal) plus most of the 15 Low findings. Also discovered and fixed a real production gap along the way: migrations 0008/0010 were tracked as applied but had never actually run (a genuine SQL syntax bug in 0010) — `activity_prompts`/`trivia_questions` didn't exist live until this session, though 5 activities' graceful hardcoded fallbacks meant no real user-facing outage. **Session 39: 13-area platform QA audit** — fixed Live Trending Rooms (auth init), privacy bypass via Recent Activity (is_public filter), broken explore filters (real participant counts, queueMicrotask cutoff), banned-user toast inconsistency (homepage + explore), fake homepage stats (10,000+ rooms, unimplemented chat features), tournament integrity (tie rejection, TBD non-clickable, completed-match re-edit guard), Party vs Classroom distinction (classroomSafe game tagging + picker filter), and pre-existing CRLF bug in docs drift check. A handful of Low findings, room auto-expiry (new Medium Priority), and the 3 larger net-new Medium features (Visual Scoreboard, XP/Leveling, Room Settings Panel) remain queued in `TASKS.md`. **Session 40: Room auto-expiry implemented + full migration audit** — migration `0009` (its `is_member_of_room` helper, hardened RLS select policies, participant-limit trigger, and the `cleanup_inactive_rooms()` function) was, like `0008`/`0010` before it, tracked as "applied" but had never actually executed live; re-running it for real deleted 23 genuinely abandoned rooms on the spot. New migration `0020` enables `pg_cron` and schedules `cleanup_inactive_rooms()` every 30 minutes, closing the room-persists-indefinitely gap from Session 39 without needing Supabase dashboard access. Followed up by systematically cross-checking all 20 migrations' expected live objects against the actual database (functions, triggers, policies, constraints, indexes, extensions, realtime publication membership, replica identity, seed-data row counts) — confirmed no further tracked-but-never-ran gaps exist beyond the three already found and fixed (`0008`, `0009`, `0010`).

**Session 45: Full Product/UX/Engineering/Security/Production-Readiness Audit (15-phase, 5 parallel domain agents) + fixing all findings tier-by-tier — COMPLETE.** Re-verified all 64 previously-fixed findings from Sessions 37-43 (all still correctly in place). Found 42 new findings (1 Critical, 4 High, 9 Medium, 28 Low). **Critical tier — fully fixed and verified:** Supabase Realtime Broadcast/Presence channels (`room:{code}`) had zero authorization, letting any anonymous session observe/forge events in any room including private ones, and letting a banned/kicked user keep full realtime access. Fixed via migration `0036` (Realtime Authorization — RLS on `realtime.messages`, scoped by the existing `is_member_of_room()` helper) plus a client change creating the channel with `{ config: { private: true } }`, gated on a new `participantRowReady` flag so a client never subscribes before its own `room_participants` row exists. Live e2e verification (Docker/WSL installed mid-session specifically for this, giving access to the project's own ephemeral local Supabase stack after live testing against production proved confounded by rate-limit pressure) caught two secondary bugs this introduced before they could ship: (1) delaying the host's own subscribe widened a window where a fast guest-join event could be missed — closed with a one-time reconciliation fetch on `SUBSCRIBED`; (2) more seriously, it widened a race in the pre-existing presence-reconciliation logic (migration 0019), causing a real, reproducible duplicate-host bug (confirmed via direct DB inspection: two rows with `role='host'` in the same room) — fixed by skipping the crash-reconciliation write on each channel's first sync only. **High (4/4), Medium (8/9), Low (26/28)** — full detail in `TASKS.md`'s Session 45 section and `CHANGELOG_AI.md`. Migrations `0036`-`0039` all applied and verified live. `npm run verify`/`npm run build` clean throughout; 7/7 Playwright tests pass repeatably against a freshly-reset local instance.

**Session 46: Post-Session-45 CI fixes + all 5 remaining Session 45 findings — COMPLETE.** Checking CI status on Session 45's push found `validate` failing; investigated rather than assumed a Session 45 regression, and found two unrelated pre-existing bugs, both fixed and pushed as their own commits first: (1) `validate` had failed since `752295f` (2026-07-06) — a build-time env var assertion in `next.config.ts` conflicted with that job's intentional no-Supabase demo-mode build, fixed with an explicit `SKIP_ENV_VALIDATION` opt-out; (2) that fix let the job's tests actually run for the first time in days, surfacing a second bug — demo-mode rooms never auto-activated their chosen game (dead code since `c0f1798`, 2026-07-04), fixed by removing a redundant outer effect guard that made an existing demo-mode branch unreachable. Then, at the user's request, all 5 items Session 45 had left open were resolved: **analytics/telemetry** (migration `0041`, `analytics_events` table — insert-only RLS, no select policy, rate-limited — plus `src/lib/analytics.ts`; deliberately scoped to 3 events: `room_created`/`room_joined`/`activity_started`, not full instrumentation); **homepage 3D hero unconditional load** (now gated on `useReducedMotion()` + an `IntersectionObserver`, so the WebGL render loop stops once scrolled out of view); **departed/kicked users' chat showing "Guest"** (migration `0040` adds a `username` snapshot column to `chat_messages`, captured at send time); **Truth or Dare content fork** (unified via a shared `TRUTH_OR_DARE_CATEGORIES` source in `src/lib/utils.ts`, without reworking the tool page's zero-network-dependency architecture as originally feared); **`tm_teams`/`nd_winner` event-kind naming** (renamed to `team_maker_teams`/`name_draw_winner`, with both old kind strings kept as read-only legacy `ActivityEvent` union members so already-persisted `room_activity_state` rows still replay). All migrations applied and verified against a local Docker Supabase stack (`supabase db reset` + full Playwright run, 7/7 passing); directly confirmed via `psql` that `analytics_events` and `chat_messages.username` are populated by real app usage. `npm run verify`/`npm run build` clean with production credentials restored. Local Docker/Supabase stack stopped after verification; Docker itself kept installed (permanent part of this repo's verification toolkit, matching `ARCHITECTURE.md` §9's `db-integration` CI job). A follow-up fix landed the same day: joining a 2-person-capped room as a genuine 2nd participant raised a false "room full" error — `check_room_limit_before_join()` counted the joining user's own row against their own capacity check on a redundant upsert (reliably reproduced via React Strict Mode's dev-only double-invocation); fixed via migration `0042`, excluding the caller's own row from the count.

**Session 47: Unban UI, empty-state copy pass, expanded e2e coverage — COMPLETE.** Closed the 3 remaining smaller items from the audit's UX×10/Product×10 lists — host-facing unban list (Session 45 deferred), consistent empty-state copy across all 14 activities (never addressed), reconnect/presence-reconciliation e2e coverage (Session 45 left open) — explicitly scoped away from the 4 larger net-new features and 2 user-action-only items. Planned via 3 parallel Explore agents + a Plan agent before implementation. Delivered: migration `0043` (host-scoped select/delete policies + `username` snapshot on `room_bans`) and a new `unban-panel.tsx` cloning `MessageReportsPanel`'s pattern; standardized "waiting for host" copy across all 14 activities plus host-idle captions for the 7 that had none, with genuine "not enough players" guards added only to Team Maker and RPS (the only 2 where 0/1 online participants produced a broken/misleading result — the other 9 work correctly solo); 2 new `multiplayer-loop.spec.ts` tests (reconnect via same-context `page.reload()`, presence-reconciliation via a 3rd participant joining then leaving cleanly). **3 real, previously-unknown bugs found and fixed via actually running the new code**: (1) first-time joiners of an in-progress room never saw the in-progress activity state — `room_activity_state`'s RLS requires an existing participant row, but `loadRoomDetails()` read it before `trackSelf()` created that row; fixed by reordering `runSetup`. (2) `room_bans` was never added to the `supabase_realtime` publication (unlike `message_reports`), so the new unban panel's list could never live-update; fixed in migration `0043`. (3) realtime-joined participants got a malformed `participants` entry (missing the nested `user` object, breaking their displayed name/avatar) because the `postgres_changes` INSERT handler pushed the raw DB row straight into state instead of shaping it like `loadParticipants()` does; fixed to match. All migrations applied fresh via local Docker Supabase, full Playwright suite (9/9) run twice; unban feature manually smoke-tested end-to-end (this manual pass is what caught bugs #2 and #3). `npm run verify` clean; migration `0043` pushed live and re-verified.

**Session 48: Audit Issues Implementation — COMPLETE.** Completed the tier-by-tier fix mandate for all issues identified in the comprehensive production readiness audit:
- **Phase 1 (DB, Backend, Security)**: Resolved the Explore page table-wide subscriptions DDOS vector by adding a denormalized `rooms.participant_count` column (migration `0044`) and triggers on `room_participants`. Secured Trivia question answers by revoking SELECT privileges on `trivia_questions(correct_index)` (migration `0045`) and verifying guesses via a secure database RPC.
- **Phase 2 (Reliability, UX & Error Recovery)**: Refactored host election to execute via an atomic database RPC (`elect_room_host`, migration `0046`), preventing split-brain states; added layout-level solo play warning banners (`src/app/tools/layout.tsx`) to all `/tools/*` pages; optimized chat performance by extracting and memoizing `ChatMessageItem` to stop typing keystroke re-render cascades; synchronized Bingo card layouts in `localStorage` keyed by room code to survive page reloads and reconnects.
- **Phase 3 (Accessibility, Polish & UX)**: Implemented "Recently Visited Rooms" list on the home screen synced to `localStorage`; converted room join headers to semantic `<label>` tags and added high-visibility keyboard focus rings; integrated "Leave Room" buttons/dialog modals in the room header and registered `beforeunload` listeners to prevent accidental session loss.
All migrations applied locally, database types regenerated, and successfully pushed to the remote production instance. The entire Playwright E2E suite is green and verified.

---

**Session 41: Full Production Readiness Audit (8-perspective, 5 parallel domain agents) + fixing all findings tier-by-tier.** User requested a comprehensive audit as if launching to real public traffic — Security, Performance/Scalability, Reliability/Production-Engineering, QA/Functional, and UX/Accessibility were each run as independent research agents, then synthesized into one report (60 findings: 4 Critical, 12 High, 16 Medium, 21 Low, 7 Nice-to-have), published as an artifact. The Critical security finding (a live DB password leaked in git history) is being rotated by the user directly. User then asked to fix everything, tier by tier, starting with Critical. **Critical tier fixes landed:** (1) a production build-time guard + unmissable banner if Supabase env vars are ever missing from a production build (previously silently degraded every visitor to same-browser-only mode with zero visible sign anything was wrong); (2) Explore page hardened for scale — `.limit(60)` added, its realtime subscription scoped to `is_public=eq.true` and debounced (was refetching the entire unbounded dataset on every single room/participant change anywhere in the app), plus a supporting partial index (`0022`); (3) **`rooms.activity_state jsonb`** (`0023`) — a capped, ordered per-activity event log that any client replays on mount/reconnect via `registerEventListener`, generically recovering all 14 activities' in-progress state after a refresh/tab-background with zero per-activity code, verified live end-to-end with a real Playwright run against the production Supabase project (question state confirmed to survive an actual page reload). **Also found and fixed, unplanned:** while functionally verifying the above, discovered migration `0019`'s `participants_update` RLS policy directly self-referenced `room_participants` (instead of routing through the safe `is_member_of_room()` security-definer helper migration `0009` established for exactly this purpose) — Postgres was rejecting every UPDATE to that table with "infinite recursion detected in policy for relation room_participants," a genuine 500 error breaking reconnects, presence sync, and host election in production. Fixed via migration `0024`, confirmed live (room join + trivia start worked cleanly afterward with zero console errors). This was NOT surfaced by any of the 5 audit agents (all static/read-only analysis) — only caught because the activity-state fix's verification step actually drove a live browser session against the real database instead of trusting typecheck alone. Also closed a small unrelated drift noted during that audit: migration `0021` tightens `room_participants_role_check` to drop the dead `'spectator'` value (client-side `UserRole.spectator` was removed as dead code back in Session 38, but the DB constraint was never updated to match) — verified zero live rows used it before applying.

---

## Overall Progress

All planned modularisation (14/14 activities), invite and QR sharing systems, realtime discovery feeds, client profile synchronizations, database-driven activity prompt migrations, security hardening, CI/dependency automation, and documentation-workflow work is complete. Build/lint/typecheck are clean. Two previously-undetected bugs were found and fixed via CI investigation: the double-elimination tournament bracket (Session 33) and demo-mode room activation (Session 34). No other known regressions.

---

## Current Objective

All room joining flows, discovery pages, profile state sync optimizations, database prompts migrations, and workflow rules are complete. Ready for new goals or feature requests.

---

## Current Focus

All pre-launch hardening tiers are complete, including Session 45's full audit, the 5 items Session 46 resolved, and the 3 smaller UX/Product items Session 47 resolved (unban UI, empty-state copy consistency, reconnect/presence-reconciliation e2e coverage) — see `TASKS.md`. Remaining: 3 net-new Medium Priority features (Visual Scoreboard, XP/Leveling System, Room Settings Panel), a Moderation Dashboard (also net-new, beyond direct SQL querying), and Production Error Monitoring (explicitly deferred — do not start unprompted). Also remaining, but require the user's own action, not code: choosing a deployment target, confirming the Supabase backup/DR plan in the dashboard. First-party analytics now exists (`analytics_events`, migration `0041`) — if extending it, keep it first-party only, given the cookie banner's no-third-party-tracking promise.

**Workflow change for future sessions:** the Supabase CLI is now linked to the live project (`qjxaehxwuqntyqrdmihs`) — `supabase/config.toml` was created via `supabase init`, the user ran `supabase login` once, and the AI ran `supabase link` + `supabase db push` directly. Future migrations no longer need manual copy-paste into the Supabase Dashboard SQL Editor; run `npx supabase db push --linked --yes` after adding a new migration file. Note: the remote migration-history table was out of sync with reality before this (many migrations 0001-0011 were originally applied by hand) — this was fixed once via `supabase migration repair --status applied <versions> --linked`. **Session 38 caveat:** "applied" in the tracking table does not guarantee the SQL actually ran successfully — `migration repair --status applied` just edits the bookkeeping, it doesn't execute anything. If a later migration touching the same table fails with "relation does not exist," don't assume it's an ordering issue; check the earlier migration's SQL for a real bug and verify the table exists via a direct REST query.

---

## Known Issues

- **`eslint` intentionally pinned at `^9`.** Dependabot will keep proposing `^10`; do not accept until `eslint-config-next`'s bundled `eslint-plugin-react` ships ESLint 10 support upstream. Confirmed (Session 35) that upgrading crashes `npm run lint` with `TypeError: contextOrFilename.getFilename is not a function` — a removed ESLint 10 API that plugin still calls internally. Verify by installing the bump in a scratch worktree and running `npm run lint` directly, not just by trusting a green CI badge on an unrelated branch.
- **Rate limiting and bans (migrations 0011/0012) are bypassable by rotating the anonymous session.** Both key on `auth.uid()`; since every user is an anonymous, unverified session, clearing browser storage (or an incognito window) resets both instantly. This is an accepted architectural trade-off (frictionless onboarding was a deliberate choice, see migration `0001`'s header), not a bug in the trigger logic — but treat it as "slows down casual abuse," not a real defense against a motivated abuser. Found and explicitly documented in the Session 37 audit; previously an implicit assumption, not written down anywhere.
- **Local CRLF reproduction of CI is unreliable — use `git show`, not clone/checkout.** This machine's global `core.autocrlf=true` smudges LF→CRLF on *any* operation that materializes files on disk (`git clone`, `git archive`, `git checkout-index`), not just the primary working tree — so a "fresh clone to /tmp" still doesn't show what a real Linux CI checkout sees. The only reliable way to inspect true blob content is `git show HEAD:<path>` (or `git cat-file`), which bypasses the smudge filter entirely. Learned the hard way in Session 37 when a genuine `docs:check` failure (a missing migration in `ARCHITECTURE.md`'s table) was initially misdiagnosed as the usual CRLF false positive.
- **Trivia's answer key is world-readable via RLS.** `trivia_questions.correct_index` has a `using (true)` select policy, so any client can fetch the answer before playing. Not fixed — would need a server-side answer-check RPC, disproportionate effort for a casual trivia game at this scale. Documented in `TASKS.md`, found in the Session 37 audit.
- **Chat profanity filter is client-side only.** `src/lib/chat-filter.ts` runs before insert client-side; a direct Supabase call bypasses it entirely. Not fixed, same reasoning as above (low severity, disproportionate effort to fix properly). Documented in `TASKS.md`.
- **Host-election "earliest joined" tiebreak isn't DB-enforced.** The client picks the earliest-joined online participant to self-promote, but the DB trigger only checks "no other online host exists" — a race could let any online participant self-promote first, not necessarily the earliest. Rare, cosmetic fairness issue only, not a security concern. Not fixed.
- **Multiple room membership is not prevented.** A single anonymous user can join multiple rooms simultaneously — there is no server-side enforcement preventing this. Accepted architectural trade-off of the anonymous identity model (frictionless onboarding over strict session constraints). Documented in Session 39; not a launch blocker.
- ~~Rooms persist indefinitely~~ — **fixed Session 40.** `pg_cron` now runs `public.cleanup_inactive_rooms()` (rooms with no online participants, >2h old) every 30 minutes via migration `0020`.
- **A real, already-merged "Session 44" exists in `git log` (13+ commits: `.gitattributes` CRLF fix, 15 High-severity fixes, 5 Medium batches, 4 Low batches, `EmptyState`/`Skeleton` components, build-time env assertion) whose work was never reflected in `AI_CONTEXT.md`/`TASKS.md`/`HANDOFF.md`/`CHANGELOG_AI.md` at all — discovered mid-Session-45 (this session), after already labeling this session's own work "Session 44" and having to rename every reference to "Session 45" to avoid colliding with it. The content of that real Session 44 closely resembles what these docs already narrate under "Session 41" (same tier structure: High → Medium batches → Low batches) — it's unclear whether it's a genuinely separate, undocumented session or a numbering mismatch between two different AI tools describing the same work under different labels. Not resolved this session (would require deeper git archaeology than this audit-fix pass had scope for) — a future session should reconcile this before trusting the exact session count anywhere in these docs.

---

## Current Assumptions

Load-bearing assumptions a new session should be aware of before making changes:

- **No verified user identity.** Every client is a Supabase anonymous auth session (`auth.uid()`) or, if Supabase isn't configured, a random `localStorage` ID — never a verified account. RLS restricts *what shape of data* a request can touch, but cannot confirm a client is who it claims to be. Treat this as "good enough to stop casual abuse," not a secure identity system (see migration `0001`'s header comment).
- **BroadcastChannel fallback is same-browser-only.** When Supabase env vars are absent, multiplayer "works" only across tabs in the same browser — it does not sync across devices or real users. Local-dev/demo convenience only.
- **No session persistence across visits.** Closing a room tab and reopening it does not restore a prior session beyond whatever `localStorage` already holds.
- **Static prompt/question content is English-only, no i18n.**

---

## Next Recommended Task

**Sessions 45, 46, and 47's full audits are complete across all tiers** (Critical/High/Medium/Low, see `TASKS.md` for exact status of every finding — nothing remains open or deferred from any of the three). What's left is genuinely net-new feature work or requires the user's own action, not further bug-fixing:

1. **Visual Scoreboard** — persistent real-time leaderboard during trivia/activities (closes the gap Trivia's own copy already implies)
2. **XP and Leveling System** — XP rewards engine with player ranks
3. **Room Settings Panel** — host-configurable settings (max participants, chat moderation, activity timers)
4. **Moderation Dashboard** — beyond direct SQL-editor querying (Product×10 item, also net-new)

Production Error Monitoring remains explicitly deferred by the user's own choice — do not start unprompted. Choosing a deployment target and confirming the Supabase backup/DR plan both require the user's own action (not code).

---

## See Also

- **Architecture, tech stack, DB schema/ER diagram, coding standards** → `ARCHITECTURE.md`
- **Why a past decision was made** → `DECISIONS.md`
- **Full chronological implementation history** → `CHANGELOG_AI.md`
- **Exactly where the previous session stopped** → `HANDOFF.md`
- **Backlog, technical debt, roadmap, priority** → `TASKS.md`
